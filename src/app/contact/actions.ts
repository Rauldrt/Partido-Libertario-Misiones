
"use server";

// This server action is no longer in use as the contact form 
// has been replaced by an embedded Google Form.
export async function submitContactForm() {
  return { success: false, message: "This form is no longer in use." };
}
