/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'i.pinimg.com',
      // Add other hostnames here if needed
      'placehold.co',
    ],
  },
};

module.exports = nextConfig;